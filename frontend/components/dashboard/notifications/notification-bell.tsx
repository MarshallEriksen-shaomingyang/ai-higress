"use client";

import { NotificationPopover } from "./notification-popover";

/**
 * 通知铃铛组件 - 用于顶部导航栏
 * 这是 NotificationPopover 的别名，提供更语义化的命名
 */
export function NotificationBell() {
  return <NotificationPopover />;
}