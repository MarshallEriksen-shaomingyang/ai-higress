# Next.js 环境变量示例

# API 地址
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Tunnel Gateway WebSocket 地址（Agent 连接用），建议显式配置避免误连后端
# 示例（本地默认）：ws://localhost:8088/bridge/tunnel
# 示例（线上 HTTPS 域名）：wss://your-domain.com/bridge/tunnel
NEXT_PUBLIC_BRIDGE_TUNNEL_URL=

# 分析开关
ANALYZE=false

# 类型检查开关
SKIP_TYPE_CHECK=false
